# ODS Reporting System
Capstone Project

## How to run
1. clone this repository `git clone https://github.com/dj-diaz/laravel-backand.git`
2. cd to the repository `cd laravel-backand`
3. run `composer install`
4. run `npm install`
5. run `php artisan key:generate`
5. run `php artisan serve`